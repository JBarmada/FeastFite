/**
 * LOCAL TESTING — set `DEV_AUTH_BYPASS` to `false` to enforce real JWTs again.
 * Must match `frontend/src/config/devAuth.ts` (`DEV_USER_ID` / fake token flow).
 */
export const DEV_AUTH_BYPASS = false;

export const DEV_USER_ID = '00000000-0000-4000-8000-000000000001';
