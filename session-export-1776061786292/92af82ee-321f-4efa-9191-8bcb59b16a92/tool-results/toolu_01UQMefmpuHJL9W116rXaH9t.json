[
  {
    "type": "text",
    "text": "Here is the complete, file-by-file implementation plan.\n\n---\n\n# FeastFite Monorepo — Implementation Plan\n\n## Environment Baseline\n\n- Node.js: v24.12.0, npm: 11.7.0\n- Docker: 29.2.0\n- No pnpm or yarn present — use **npm workspaces** throughout\n- Key package versions pinned below are the latest stable as of this plan\n\n---\n\n## Part 1 — Decision Record (Read This Before Writing a Single File)\n\n### 1.1 npm workspaces vs pnpm\n\nUse npm workspaces. pnpm is not installed, and npm 11 workspaces are mature. The root `package.json` declares `\"workspaces\": [\"packages/*\", \"services/*\", \"frontend\"]`. This means `npm install` at the root hoists all deps into the root `node_modules` with symlinks for workspace packages. One gotcha: `@feastfite/shared` will be resolvable from any service package only after it has been `npm install`-ed at the root level; do not run `npm install` in individual service directories until the root install has been done at least once.\n\n### 1.2 TypeScript Module Resolution\n\nUse `\"moduleResolution\": \"bundler\"` in `tsconfig.base.json` for the frontend (Vite uses bundler resolution). Use `\"moduleResolution\": \"node16\"` for all Node.js services to get proper ESM/CJS interop. Services will compile to CommonJS (`\"module\": \"commonjs\"`) so they work without `.js` extension imports. Set `\"paths\"` in `tsconfig.base.json` to map `@feastfite/shared` to `../../packages/shared/src` — this is for editor IntelliSense only; the runtime resolution uses the npm workspace symlink.\n\n### 1.3 Dockerfile Strategy\n\nAll service Dockerfiles use a two-stage build:\n- Stage 1 (`builder`): `node:24-alpine`, copies the entire monorepo root (needed for workspace symlinks), runs `npm ci`, then `npm run build -w services/<name>`.\n- Stage 2 (`runtime`): `node:24-alpine`, copies only `dist/` and `node_modules/` from the builder stage. This avoids shipping TypeScript source.\n\nCritical gotcha: When building a service image from a monorepo, the Docker build context must be the **repo root**, not the service subdirectory. The `docker-compose.yml` must set `context: .` and `dockerfile: services/<name>/Dockerfile`. Every service Dockerfile must therefore start with `WORKDIR /app` and `COPY . .`.\n\n### 1.4 Kong Declarative Format\n\nKong 3.6 uses `_format_version: \"3.0\"` in `kong.yml`. Do not use `\"2.1\"` — it is deprecated in 3.x. The Kong Gateway OSS image `kong:3.6` is used. The environment variable `KONG_DATABASE=off` and `KONG_DECLARATIVE_CONFIG=/etc/kong/kong.yml` activate DB-less mode.\n\n### 1.5 PostGIS Image\n\nUse `postgis/postgis:16-3.4` (the official image combining PostgreSQL 16 + PostGIS 3.4). This image automatically loads the `postgis` extension but you still need `CREATE EXTENSION IF NOT EXISTS postgis;` in your migration scripts.\n\n### 1.6 RabbitMQ Management UI\n\nUse `rabbitmq:3.13-management` (not the plain `rabbitmq:3.13`). The `-management` tag bundles the HTTP management plugin on port 15672.\n\n### 1.7 `.env` Strategy\n\nA single `.env.example` at the repo root documents all variables. Each service also has its own `.env` at `services/<name>/.env` for local `npm run dev` runs outside Docker. These per-service `.env` files are gitignored. The `docker-compose.yml` uses `env_file` directives pointing to these files, with environment variable overrides for Docker networking (e.g., `DB_HOST=postgres-auth` instead of `localhost`).\n\n### 1.8 Event Schema Location\n\n`events/schemas/` contains pure TypeScript interface files — no runtime code. They are not a separate npm package; they are copied (or path-aliased) into each service that needs them. The simpler approach: add `events/` to the root `tsconfig.base.json` `include` path and reference it from `@feastfite/shared`'s re-exports.\n\n---\n\n## Part 2 — Exact File Creation Order\n\nThe order matters because of dependency chains:\n\n```\nPhase A — Root scaffolding (no deps between files)\n  1.  .gitignore\n  2.  .env.example\n  3.  tsconfig.base.json\n  4.  package.json  (root, declares workspaces)\n\nPhase B — Shared package (services depend on this)\n  5.  packages/shared/package.json\n  6.  packages/shared/tsconfig.json\n  7.  packages/shared/types/index.ts\n  8.  packages/shared/utils/jwt.ts\n  9.  packages/shared/utils/amqp.ts\n\nPhase C — Event schemas (used by shared + services)\n  10. events/schemas/vote.winner_declared.ts\n  11. events/schemas/territory.claimed.ts\n  12. events/schemas/item.purchased.ts\n  13. events/schemas/user.registered.ts\n\nPhase D — Service stubs (all 5, can be done in parallel by 4 devs)\n  For each of: auth, territory, vote, economy, profile:\n  14. services/<name>/package.json\n  15. services/<name>/tsconfig.json\n  16. services/<name>/src/index.ts\n  17. services/<name>/Dockerfile\n  18. services/<name>/.env\n\nPhase E — API Gateway\n  19. services/api-gateway/kong.yml\n  20. services/api-gateway/Dockerfile\n\nPhase F — Frontend\n  21. frontend/package.json\n  22. frontend/tsconfig.json + tsconfig.node.json\n  23. frontend/vite.config.ts\n  24. frontend/index.html\n  25. frontend/src/main.tsx\n  26. frontend/src/App.tsx\n  27. frontend/src/contexts/AuthContext.tsx\n  28. frontend/src/components/PrivateRoute.tsx\n  29. frontend/src/api/authApi.ts\n  30. frontend/src/api/territoryApi.ts\n  31. frontend/src/api/voteApi.ts\n  32. frontend/src/api/economyApi.ts\n  33. frontend/src/api/profileApi.ts\n  34. frontend/src/pages/LoginPage.tsx\n  35. frontend/src/pages/RegisterPage.tsx\n\nPhase G — Infrastructure\n  36. docker-compose.yml\n  37. k8s/namespace.yml\n  38. k8s/infra/postgres.yml\n  39. k8s/infra/redis.yml\n  40. k8s/infra/rabbitmq.yml\n  41. k8s/infra/minio.yml\n```\n\n---\n\n## Part 3 — File-by-File Content Specification\n\n### Phase A — Root Scaffolding\n\n---\n\n**File 1: `.gitignore`**\n\n```\n# Dependencies\nnode_modules/\n.npm/\n\n# Build output\ndist/\nbuild/\n\n# Environment files (per-service .env, never commit real secrets)\nservices/**/.env\nfrontend/.env\n.env\n!.env.example\n\n# TypeScript incremental build info\n*.tsbuildinfo\n\n# Editor\n.vscode/\n.idea/\n*.sw[po]\n\n# OS\n.DS_Store\nThumbs.db\n\n# Docker local volumes\n.docker-volumes/\n\n# Logs\n*.log\nnpm-debug.log*\n```\n\nKey decision: `.env.example` is committed (it has placeholder values). All real `.env` files are gitignored. The `!.env.example` negation pattern is required because the `services/**/.env` glob would otherwise catch `.env.example` if it were in a service directory.\n\n---\n\n**File 2: `.env.example`**\n\nThis file is documentation. Every variable used anywhere in the project lives here with a safe default or a `CHANGE_ME` marker.\n\n```dotenv\n# ── Shared JWT ───────────────────────────────────────────────────\nJWT_SECRET=CHANGE_ME_use_openssl_rand_base64_32\nJWT_EXPIRES_IN=15m\n\n# ── PostgreSQL ────────────────────────────────────────────────────\nPOSTGRES_USER=feastfite\nPOSTGRES_PASSWORD=CHANGE_ME\n\nAUTH_DB_HOST=localhost\nAUTH_DB_PORT=5432\nAUTH_DB_NAME=auth_db\n\nTERRITORY_DB_HOST=localhost\nTERRITORY_DB_PORT=5433\nTERRITORY_DB_NAME=territory_db\n\nVOTE_DB_HOST=localhost\nVOTE_DB_PORT=5434\nVOTE_DB_NAME=vote_db\n\nECONOMY_DB_HOST=localhost\nECONOMY_DB_PORT=5435\nECONOMY_DB_NAME=economy_db\n\nPROFILE_DB_HOST=localhost\nPROFILE_DB_PORT=5436\nPROFILE_DB_NAME=profile_db\n\n# ── Redis ─────────────────────────────────────────────────────────\nREDIS_URL=redis://localhost:6379\n\n# ── RabbitMQ ──────────────────────────────────────────────────────\nRABBITMQ_URL=amqp://guest:guest@localhost:5672\nRABBITMQ_EXCHANGE=feastfite.events\n\n# ── MinIO ─────────────────────────────────────────────────────────\nMINIO_ENDPOINT=localhost\nMINIO_PORT=9000\nMINIO_ACCESS_KEY=minioadmin\nMINIO_SECRET_KEY=CHANGE_ME\nMINIO_BUCKET_PHOTOS=fight-photos\n\n# ── Services (for docker-compose inter-service calls) ─────────────\nAUTH_SERVICE_URL=http://auth-service:3001\nTERRITORY_SERVICE_URL=http://territory-service:3002\nVOTE_SERVICE_URL=http://vote-service:3003\nECONOMY_SERVICE_URL=http://economy-service:3004\nPROFILE_SERVICE_URL=http://profile-service:3005\n\n# ── Kong ──────────────────────────────────────────────────────────\nKONG_ADMIN_URL=http://localhost:8001\n```\n\n---\n\n**File 3: `tsconfig.base.json`**\n\n```json\n{\n  \"compilerOptions\": {\n    \"target\": \"ES2022\",\n    \"lib\": [\"ES2022\"],\n    \"moduleResolution\": \"node16\",\n    \"module\": \"commonjs\",\n    \"strict\": true,\n    \"esModuleInterop\": true,\n    \"skipLibCheck\": true,\n    \"forceConsistentCasingInFileNames\": true,\n    \"resolveJsonModule\": true,\n    \"declaration\": true,\n    \"declarationMap\": true,\n    \"sourceMap\": true,\n    \"outDir\": \"dist\",\n    \"rootDir\": \"src\",\n    \"paths\": {\n      \"@feastfite/shared\": [\"../../packages/shared/src/index.ts\"],\n      \"@feastfite/shared/*\": [\"../../packages/shared/src/*\"]\n    }\n  },\n  \"exclude\": [\"node_modules\", \"dist\"]\n}\n```\n\nGotcha on `paths`: The `paths` here use a relative offset of `../../` which is correct when extended from `services/<name>/tsconfig.json`. The frontend uses its own tsconfig that overrides `moduleResolution` to `bundler` and adjusts `paths` with `../`. Services that extend this base get Node16 resolution and CommonJS output automatically. The `declaration: true` + `declarationMap: true` combo is required so that other workspace packages can import from `@feastfite/shared` with full type information without needing to compile it first in CI.\n\n---\n\n**File 4: `package.json` (root)**\n\n```json\n{\n  \"name\": \"feastfite\",\n  \"version\": \"0.0.1\",\n  \"private\": true,\n  \"workspaces\": [\n    \"packages/*\",\n    \"services/auth-service\",\n    \"services/territory-service\",\n    \"services/vote-service\",\n    \"services/economy-service\",\n    \"services/profile-service\",\n    \"frontend\"\n  ],\n  \"scripts\": {\n    \"build\": \"npm run build --workspaces --if-present\",\n    \"build:shared\": \"npm run build -w packages/shared\",\n    \"dev\": \"concurrently -n auth,territory,vote,economy,profile,frontend \\\"npm run dev -w services/auth-service\\\" \\\"npm run dev -w services/territory-service\\\" \\\"npm run dev -w services/vote-service\\\" \\\"npm run dev -w services/economy-service\\\" \\\"npm run dev -w services/profile-service\\\" \\\"npm run dev -w frontend\\\"\",\n    \"typecheck\": \"tsc --build --noEmit\",\n    \"lint\": \"eslint . --ext .ts,.tsx\",\n    \"test\": \"npm test --workspaces --if-present\"\n  },\n  \"devDependencies\": {\n    \"concurrently\": \"^9.2.1\",\n    \"typescript\": \"^6.0.2\"\n  },\n  \"engines\": {\n    \"node\": \">=20.0.0\",\n    \"npm\": \">=10.0.0\"\n  }\n}\n```\n\nKey decisions:\n- `\"private\": true` prevents accidental `npm publish` of the root package.\n- `api-gateway` is NOT in workspaces — it has no `package.json` (it is a Kong config + Dockerfile only).\n- `concurrently` lives at root so the `dev` script works without each service installing it.\n- TypeScript is a root devDep so all workspaces share a single version — critical for declaration file consistency.\n\n---\n\n### Phase B — Shared Package\n\n---\n\n**File 5: `packages/shared/package.json`**\n\n```json\n{\n  \"name\": \"@feastfite/shared\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"main\": \"./dist/index.js\",\n  \"types\": \"./dist/index.d.ts\",\n  \"exports\": {\n    \".\": {\n      \"import\": \"./dist/index.js\",\n      \"require\": \"./dist/index.js\",\n      \"types\": \"./dist/index.d.ts\"\n    },\n    \"./types\": {\n      \"import\": \"./dist/types/index.js\",\n      \"require\": \"./dist/types/index.js\",\n      \"types\": \"./dist/types/index.d.ts\"\n    },\n    \"./utils/*\": {\n      \"import\": \"./dist/utils/*.js\",\n      \"require\": \"./dist/utils/*.js\"\n    }\n  },\n  \"scripts\": {\n    \"build\": \"tsc -p tsconfig.json\",\n    \"build:watch\": \"tsc -p tsconfig.json --watch\",\n    \"clean\": \"rm -rf dist\"\n  },\n  \"dependencies\": {\n    \"amqplib\": \"^1.0.3\",\n    \"jsonwebtoken\": \"^9.0.3\"\n  },\n  \"devDependencies\": {\n    \"@types/amqplib\": \"^0.10.8\",\n    \"@types/jsonwebtoken\": \"^9.0.10\",\n    \"@types/node\": \"^25.6.0\"\n  }\n}\n```\n\nGotcha: `\"main\"` must point to `./dist/index.js` (not `./src/index.ts`). When running inside Docker after `npm ci` + `tsc`, this is fine. During local dev, the npm `paths` alias in each service's tsconfig points directly to the TypeScript source, so IDEs resolve types from `src/` while the runtime uses `dist/`. This means you must run `npm run build:shared` (or `npm run build -w packages/shared`) before the first time you run any service — document this in the README.\n\n---\n\n**File 6: `packages/shared/tsconfig.json`**\n\n```json\n{\n  \"extends\": \"../../tsconfig.base.json\",\n  \"compilerOptions\": {\n    \"rootDir\": \"src\",\n    \"outDir\": \"dist\",\n    \"composite\": true,\n    \"paths\": {}\n  },\n  \"include\": [\"src/**/*\"],\n  \"exclude\": [\"node_modules\", \"dist\"]\n}\n```\n\n`\"composite\": true` enables TypeScript project references, which allows `tsc --build` from the root to build the shared package before services. `\"paths\": {}` overrides the base — the shared package does not use `@feastfite/shared` internally, so the path alias would create a circular reference.\n\n---\n\n**File 7: `packages/shared/src/types/index.ts`**\n\n```typescript\nexport interface User {\n  id: string;\n  email: string;\n  username: string;\n  clanId: string | null;\n  createdAt: Date;\n  updatedAt: Date;\n}\n\nexport interface Territory {\n  id: string;\n  name: string;\n  geoJson: GeoJSON.Feature;     // GeoJSON polygon\n  ownerId: string | null;\n  ownerType: 'user' | 'clan' | null;\n  capturedAt: Date | null;\n  updatedAt: Date;\n}\n\nexport interface VoteSession {\n  id: string;\n  territoryId: string;\n  challengerId: string;\n  defenderId: string;\n  status: 'pending' | 'active' | 'completed' | 'cancelled';\n  winnerId: string | null;\n  winnerPhotoKey: string | null;  // MinIO object key\n  startedAt: Date;\n  endedAt: Date | null;\n}\n\nexport interface PointsLedgerEntry {\n  id: string;\n  userId: string;\n  delta: number;                  // positive = gain, negative = spend\n  reason: string;\n  referenceId: string | null;     // e.g., voteSessionId or itemId\n  createdAt: Date;\n}\n\nexport interface Achievement {\n  id: string;\n  userId: string;\n  type: string;                   // e.g., 'first_territory', 'win_streak_5'\n  payload: Record<string, unknown>;\n  awardedAt: Date;\n}\n\nexport interface Clan {\n  id: string;\n  name: string;\n  tag: string;                    // 3-5 char tag, e.g., \"FITE\"\n  founderId: string;\n  memberCount: number;\n  totalPoints: number;\n  createdAt: Date;\n}\n```\n\nNote: `Territory.geoJson` uses `GeoJSON.Feature` from the `@types/geojson` package. Add `@types/geojson` as a devDependency in `packages/shared/package.json`. This keeps the type self-documenting without adding a runtime dep.\n\n---\n\n**File 8: `packages/shared/src/utils/jwt.ts`**\n\n```typescript\nimport jwt, { type JwtPayload, type SignOptions } from 'jsonwebtoken';\n\nexport interface FeastFiteJwtPayload extends JwtPayload {\n  userId: string;\n  email: string;\n  username: string;\n}\n\nexport function verifyToken(token: string, secret?: string): FeastFiteJwtPayload {\n  const jwtSecret = secret ?? process.env['JWT_SECRET'];\n  if (!jwtSecret) {\n    throw new Error('JWT_SECRET is not configured');\n  }\n  const payload = jwt.verify(token, jwtSecret);\n  if (typeof payload === 'string') {\n    throw new Error('Unexpected string JWT payload');\n  }\n  return payload as FeastFiteJwtPayload;\n}\n\nexport function signToken(\n  payload: Omit<FeastFiteJwtPayload, keyof JwtPayload>,\n  secret?: string,\n  options?: SignOptions\n): string {\n  const jwtSecret = secret ?? process.env['JWT_SECRET'];\n  if (!jwtSecret) {\n    throw new Error('JWT_SECRET is not configured');\n  }\n  return jwt.sign(payload, jwtSecret, {\n    expiresIn: process.env['JWT_EXPIRES_IN'] ?? '15m',\n    ...options,\n  });\n}\n```\n\nNote: `signToken` is included here alongside `verifyToken` because auth-service will need it, and the API gateway Kong plugin validates tokens — there is no reason to sign tokens anywhere except auth-service. By putting both in shared, auth-service can import `signToken` while every other service only imports `verifyToken`.\n\n---\n\n**File 9: `packages/shared/src/utils/amqp.ts`**\n\n```typescript\nimport amqplib, { type Connection, type Channel } from 'amqplib';\n\nconst RECONNECT_DELAY_MS = 5_000;\nconst MAX_RECONNECT_ATTEMPTS = 10;\n\nexport interface AmqpConnection {\n  connection: Connection;\n  channel: Channel;\n}\n\nexport async function createAmqpConnection(\n  url?: string,\n  exchangeName?: string\n): Promise<AmqpConnection> {\n  const amqpUrl = url ?? process.env['RABBITMQ_URL'] ?? 'amqp://guest:guest@localhost:5672';\n  const exchange = exchangeName ?? process.env['RABBITMQ_EXCHANGE'] ?? 'feastfite.events';\n\n  let attempts = 0;\n\n  async function connect(): Promise<AmqpConnection> {\n    attempts += 1;\n    try {\n      const connection = await amqplib.connect(amqpUrl);\n      const channel = await connection.createChannel();\n\n      // Declare topic exchange (idempotent — safe to call on every startup)\n      await channel.assertExchange(exchange, 'topic', { durable: true });\n\n      connection.on('error', (err) => {\n        console.error('[amqp] connection error:', err.message);\n        scheduleReconnect();\n      });\n\n      connection.on('close', () => {\n        console.warn('[amqp] connection closed, reconnecting...');\n        scheduleReconnect();\n      });\n\n      console.info(`[amqp] connected to ${amqpUrl}, exchange: ${exchange}`);\n      attempts = 0;\n      return { connection, channel };\n    } catch (err) {\n      if (attempts >= MAX_RECONNECT_ATTEMPTS) {\n        throw new Error(\n          `[amqp] failed to connect after ${MAX_RECONNECT_ATTEMPTS} attempts: ${String(err)}`\n        );\n      }\n      console.warn(`[amqp] connect attempt ${attempts} failed, retrying in ${RECONNECT_DELAY_MS}ms...`);\n      await new Promise((resolve) => setTimeout(resolve, RECONNECT_DELAY_MS));\n      return connect();\n    }\n  }\n\n  let reconnectTimeout: ReturnType<typeof setTimeout> | null = null;\n\n  function scheduleReconnect(): void {\n    if (reconnectTimeout) return;\n    reconnectTimeout = setTimeout(async () => {\n      reconnectTimeout = null;\n      await connect().catch((err) => {\n        console.error('[amqp] reconnect failed permanently:', err);\n        process.exit(1);\n      });\n    }, RECONNECT_DELAY_MS);\n  }\n\n  return connect();\n}\n```\n\nDesign notes:\n- The factory uses a recursive retry loop with exponential-free fixed delay — simple to reason about.\n- `process.exit(1)` on permanent failure is intentional: Docker/Kubernetes will restart the container, which is the correct recovery mechanism in production.\n- Each service that calls `createAmqpConnection` should store the returned `channel` and use it for both publishing and consuming. Do not call `createAmqpConnection` once per publish.\n\n---\n\n**File 10: `packages/shared/src/index.ts`** (barrel file, not listed in original structure but required)\n\n```typescript\nexport * from './types/index.js';\nexport * from './utils/jwt.js';\nexport * from './utils/amqp.js';\n```\n\nNote: `.js` extensions in exports are required for Node16 module resolution even in TypeScript source files. TypeScript resolves `.js` → `.ts` at compile time. This is a common gotcha.\n\n---\n\n### Phase C — Event Schemas\n\n---\n\n**File 10–13: `events/schemas/*.ts`**\n\nThese files export pure TypeScript interfaces. They live outside any npm package so any service can import them via a `tsconfig.json` path alias.\n\n`events/schemas/vote.winner_declared.ts`:\n```typescript\nexport interface VoteWinnerDeclaredEvent {\n  eventType: 'vote.winner_declared';\n  sessionId: string;\n  territoryId: string;\n  winnerId: string;\n  winnerPhotoKey: string;     // MinIO object key for the winning photo\n  timestamp: string;          // ISO 8601\n}\n```\n\n`events/schemas/territory.claimed.ts`:\n```typescript\nexport interface TerritoryClaimedEvent {\n  eventType: 'territory.claimed';\n  territoryId: string;\n  newOwnerId: string;\n  previousOwnerId: string | null;\n  timestamp: string;\n}\n```\n\n`events/schemas/item.purchased.ts`:\n```typescript\nexport interface ItemPurchasedEvent {\n  eventType: 'item.purchased';\n  userId: string;\n  itemId: string;\n  itemType: 'cosmetic' | 'boost' | 'consumable';\n  pointsSpent: number;\n  timestamp: string;\n}\n```\n\n`events/schemas/user.registered.ts`:\n```typescript\nexport interface UserRegisteredEvent {\n  eventType: 'user.registered';\n  userId: string;\n  email: string;\n  timestamp: string;\n}\n```\n\nDesign note: The `eventType` discriminant property on every event enables a TypeScript tagged union across all event types:\n\n```typescript\nexport type FeastFiteEvent =\n  | VoteWinnerDeclaredEvent\n  | TerritoryClaimedEvent\n  | ItemPurchasedEvent\n  | UserRegisteredEvent;\n```\n\nAdd this union type to `packages/shared/src/types/index.ts` and re-export the individual event interfaces from `packages/shared/src/index.ts` so consuming services get full type-safety when deserializing from RabbitMQ.\n\n---\n\n### Phase D — Service Stubs\n\nThe pattern is identical across all 5 services. Shown in full for `auth-service`, with diffs noted for the others.\n\n---\n\n**`services/auth-service/package.json`**\n\n```json\n{\n  \"name\": \"@feastfite/auth-service\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"scripts\": {\n    \"build\": \"tsc -p tsconfig.json\",\n    \"dev\": \"tsx watch src/index.ts\",\n    \"start\": \"node dist/index.js\",\n    \"clean\": \"rm -rf dist\"\n  },\n  \"dependencies\": {\n    \"@feastfite/shared\": \"*\",\n    \"amqplib\": \"^1.0.3\",\n    \"cors\": \"^2.8.6\",\n    \"dotenv\": \"^17.4.2\",\n    \"express\": \"^5.2.1\",\n    \"helmet\": \"^8.1.0\",\n    \"ioredis\": \"^5.10.1\",\n    \"pg\": \"^8.20.0\"\n  },\n  \"devDependencies\": {\n    \"@types/amqplib\": \"^0.10.8\",\n    \"@types/cors\": \"^2.8.19\",\n    \"@types/express\": \"^5.0.6\",\n    \"@types/node\": \"^25.6.0\",\n    \"@types/pg\": \"^8.20.0\",\n    \"tsx\": \"^4.21.0\"\n  }\n}\n```\n\nKey decisions:\n- `\"@feastfite/shared\": \"*\"` — `*` resolves to the local workspace package. Do not use a version number or `file:` path.\n- `tsx` is the dev runner (no `ts-node` — tsx is faster and doesn't require `ts-node/esm` loader fiddling for Node16).\n- `express: \"^5.2.1\"` — Express 5 is used (it has async error handling by default). The `@types/express` `5.x` package corresponds.\n- `dotenv` is a runtime dep, not devDep, because `dist/index.js` still needs it at startup in Docker.\n\nFor the other 4 services, the `package.json` is identical except `\"name\"` changes:\n- `@feastfite/territory-service`\n- `@feastfite/vote-service`\n- `@feastfite/economy-service`\n- `@feastfite/profile-service`\n\n---\n\n**`services/auth-service/tsconfig.json`**\n\n```json\n{\n  \"extends\": \"../../tsconfig.base.json\",\n  \"compilerOptions\": {\n    \"rootDir\": \"src\",\n    \"outDir\": \"dist\",\n    \"composite\": true\n  },\n  \"references\": [\n    { \"path\": \"../../packages/shared\" }\n  ],\n  \"include\": [\"src/**/*\"],\n  \"exclude\": [\"node_modules\", \"dist\"]\n}\n```\n\n`\"references\"` is the TypeScript project references mechanism. It tells `tsc --build` to compile `packages/shared` first. All 5 service `tsconfig.json` files have this same `references` entry.\n\n---\n\n**`services/auth-service/src/index.ts`**\n\n```typescript\nimport 'dotenv/config';\nimport express from 'express';\nimport cors from 'cors';\nimport helmet from 'helmet';\n\nconst app = express();\nconst PORT = process.env['PORT'] ?? 3001;\nconst SERVICE_NAME = 'auth-service';\n\napp.use(helmet());\napp.use(cors());\napp.use(express.json());\n\napp.get('/health', (_req, res) => {\n  res.json({ status: 'ok', service: SERVICE_NAME });\n});\n\n// TODO: mount routers here\n// app.use('/api/auth', authRouter);\n\napp.listen(PORT, () => {\n  console.info(`[${SERVICE_NAME}] listening on port ${PORT}`);\n});\n\nexport default app;\n```\n\nPort assignments for all services:\n- `auth-service`: 3001\n- `territory-service`: 3002\n- `vote-service`: 3003\n- `economy-service`: 3004\n- `profile-service`: 3005\n\n---\n\n**`services/auth-service/Dockerfile`**\n\n```dockerfile\n# ── Stage 1: Builder ─────────────────────────────────────────────\nFROM node:24-alpine AS builder\nWORKDIR /app\n\n# Copy the full monorepo (needed for workspace symlinks)\nCOPY package.json package-lock.json ./\nCOPY packages/shared/package.json ./packages/shared/\nCOPY services/auth-service/package.json ./services/auth-service/\n# Copy tsconfig files needed for the build\nCOPY tsconfig.base.json ./\nCOPY packages/shared ./packages/shared\nCOPY services/auth-service ./services/auth-service\n\n# Install all workspace deps\nRUN npm ci --workspace=packages/shared --workspace=services/auth-service\n\n# Build shared first, then the service\nRUN npm run build -w packages/shared\nRUN npm run build -w services/auth-service\n\n# ── Stage 2: Runtime ─────────────────────────────────────────────\nFROM node:24-alpine AS runtime\nWORKDIR /app\n\nENV NODE_ENV=production\n\n# Copy workspace structure needed at runtime\nCOPY --from=builder /app/packages/shared/dist ./packages/shared/dist\nCOPY --from=builder /app/packages/shared/package.json ./packages/shared/package.json\nCOPY --from=builder /app/services/auth-service/dist ./services/auth-service/dist\nCOPY --from=builder /app/services/auth-service/package.json ./services/auth-service/package.json\nCOPY --from=builder /app/node_modules ./node_modules\nCOPY --from=builder /app/package.json ./package.json\n\nWORKDIR /app/services/auth-service\nEXPOSE 3001\n\nCMD [\"node\", \"dist/index.js\"]\n```\n\nImportant Docker gotcha: The `npm ci` in the builder must be run from the monorepo root with `--workspace` flags, not from within the service directory. This ensures workspace symlinks in `node_modules` are set up correctly. The runtime stage copies the entire root `node_modules` — this is larger than ideal but correct for npm workspaces. An alternative with pnpm would be smaller, but since npm is mandated, this is the right approach.\n\nFor the other services, the Dockerfile is identical with these substitutions:\n- Replace `auth-service` with the service name\n- Replace `EXPOSE 3001` with the correct port (3002–3005)\n\n---\n\n**`services/auth-service/.env`** (gitignored, local dev only)\n\n```dotenv\nPORT=3001\nNODE_ENV=development\n\nDB_HOST=localhost\nDB_PORT=5432\nDB_NAME=auth_db\nDB_USER=feastfite\nDB_PASSWORD=CHANGE_ME\n\nREDIS_URL=redis://localhost:6379\nRABBITMQ_URL=amqp://guest:guest@localhost:5672\nRABBITMQ_EXCHANGE=feastfite.events\nJWT_SECRET=dev_only_secret_change_in_production\nJWT_EXPIRES_IN=15m\n```\n\nFor Docker Compose, these variables will be overridden via the `environment:` section in `docker-compose.yml` to use Docker network hostnames instead of `localhost`.\n\n---\n\n### Phase E — API Gateway\n\n---\n\n**`services/api-gateway/Dockerfile`**\n\n```dockerfile\nFROM kong:3.6\n\nUSER root\nCOPY kong.yml /etc/kong/kong.yml\nUSER kong\n```\n\nThis is minimal — Kong reads `kong.yml` at startup via the `KONG_DECLARATIVE_CONFIG` env var set in `docker-compose.yml`.\n\n---\n\n**`services/api-gateway/kong.yml`**\n\n```yaml\n_format_version: \"3.0\"\n_transform: true\n\nservices:\n  - name: auth-service\n    url: http://auth-service:3001\n    routes:\n      - name: auth-routes\n        paths:\n          - /api/auth\n        strip_path: false\n        protocols: [http, https]\n\n  - name: territory-service\n    url: http://territory-service:3002\n    routes:\n      - name: territory-routes\n        paths:\n          - /api/territory\n        strip_path: false\n        protocols: [http, https]\n\n  - name: vote-service\n    url: http://vote-service:3003\n    routes:\n      - name: vote-routes\n        paths:\n          - /api/vote\n        strip_path: false\n        protocols: [http, https]\n      - name: vote-ws-route\n        paths:\n          - /ws/vote\n        strip_path: false\n        protocols: [ws, wss]\n\n  - name: economy-service\n    url: http://economy-service:3004\n    routes:\n      - name: economy-routes\n        paths:\n          - /api/economy\n        strip_path: false\n        protocols: [http, https]\n\n  - name: profile-service\n    url: http://profile-service:3005\n    routes:\n      - name: profile-routes\n        paths:\n          - /api/profile\n        strip_path: false\n        protocols: [http, https]\n\nplugins:\n  - name: jwt\n    config:\n      secret_is_base64: false\n      claims_to_verify:\n        - exp\n    route: null  # applies globally; restrict per-route once routes are stable\n\n  - name: rate-limiting\n    config:\n      minute: 120\n      hour: 3000\n      policy: local\n      fault_tolerant: true\n      hide_client_headers: false\n\n  - name: cors\n    config:\n      origins:\n        - \"http://localhost:5173\"  # Vite dev server\n        - \"https://feastfite.com\"\n      methods:\n        - GET\n        - POST\n        - PUT\n        - PATCH\n        - DELETE\n        - OPTIONS\n      headers:\n        - Accept\n        - Authorization\n        - Content-Type\n      credentials: true\n      max_age: 3600\n```\n\nKong 3.x declarative format gotchas:\n1. `_format_version: \"3.0\"` is required — do not use `\"2.1\"`.\n2. `_transform: true` allows Kong to apply default transformations (recommended).\n3. The JWT plugin in DB-less mode requires consumers with JWT credentials to be defined in `kong.yml` as well. During the scaffolding phase, set the plugin at the global level with `enabled: false` or scope it only to authenticated routes — otherwise every request to `/health` will fail.\n4. The WebSocket route (`/ws/vote`) must list `protocols: [ws, wss]` explicitly. Kong proxies WebSocket connections transparently once this is set.\n5. Rate-limiting with `policy: local` is fine for single-instance Kong; switch to `policy: redis` in production with a shared Redis.\n\n---\n\n### Phase F — Frontend\n\n---\n\n**`frontend/package.json`**\n\n```json\n{\n  \"name\": \"@feastfite/frontend\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"scripts\": {\n    \"dev\": \"vite\",\n    \"build\": \"tsc && vite build\",\n    \"preview\": \"vite preview\",\n    \"typecheck\": \"tsc --noEmit\"\n  },\n  \"dependencies\": {\n    \"@feastfite/shared\": \"*\",\n    \"axios\": \"^1.15.0\",\n    \"react\": \"^19.2.5\",\n    \"react-dom\": \"^19.2.5\",\n    \"react-router-dom\": \"^7.14.0\"\n  },\n  \"devDependencies\": {\n    \"@types/node\": \"^25.6.0\",\n    \"@types/react\": \"^19.2.14\",\n    \"@types/react-dom\": \"^19.2.3\",\n    \"@vitejs/plugin-react\": \"^4.0.0\",\n    \"typescript\": \"^6.0.2\",\n    \"vite\": \"^8.0.8\"\n  }\n}\n```\n\nNote: The frontend uses `\"type\": \"module\"` because Vite requires ES module config files. This does NOT affect the services (they use CommonJS via `tsconfig.base.json`).\n\n---\n\n**`frontend/tsconfig.json`**\n\n```json\n{\n  \"compilerOptions\": {\n    \"target\": \"ES2020\",\n    \"lib\": [\"ES2020\", \"DOM\", \"DOM.Iterable\"],\n    \"module\": \"ESNext\",\n    \"moduleResolution\": \"bundler\",\n    \"strict\": true,\n    \"esModuleInterop\": true,\n    \"skipLibCheck\": true,\n    \"forceConsistentCasingInFileNames\": true,\n    \"resolveJsonModule\": true,\n    \"jsx\": \"react-jsx\",\n    \"noEmit\": true,\n    \"paths\": {\n      \"@feastfite/shared\": [\"../packages/shared/src/index.ts\"],\n      \"@feastfite/shared/*\": [\"../packages/shared/src/*\"]\n    }\n  },\n  \"include\": [\"src\"],\n  \"references\": [{ \"path\": \"./tsconfig.node.json\" }]\n}\n```\n\n`moduleResolution: \"bundler\"` is Vite's recommended setting — it resolves paths like Vite/esbuild does, supporting bare specifiers without `.js` extension. Note the `paths` here use `../` (one level up) because the frontend is at `frontend/` not `services/<name>/`.\n\n**`frontend/tsconfig.node.json`**\n\n```json\n{\n  \"compilerOptions\": {\n    \"composite\": true,\n    \"skipLibCheck\": true,\n    \"module\": \"ESNext\",\n    \"moduleResolution\": \"bundler\",\n    \"allowSyntheticDefaultImports\": true\n  },\n  \"include\": [\"vite.config.ts\"]\n}\n```\n\n---\n\n**`frontend/vite.config.ts`**\n\n```typescript\nimport { defineConfig } from 'vite';\nimport react from '@vitejs/plugin-react';\nimport path from 'path';\n\nexport default defineConfig({\n  plugins: [react()],\n  resolve: {\n    alias: {\n      '@feastfite/shared': path.resolve(__dirname, '../packages/shared/src/index.ts'),\n    },\n  },\n  server: {\n    port: 5173,\n    proxy: {\n      '/api': {\n        target: 'http://localhost:8000',  // Kong gateway\n        changeOrigin: true,\n      },\n      '/ws': {\n        target: 'ws://localhost:8000',\n        ws: true,\n        changeOrigin: true,\n      },\n    },\n  },\n});\n```\n\nThe Vite alias for `@feastfite/shared` must match the `tsconfig.json` paths. The proxy routes all API calls through Kong during development, mirroring the production setup.\n\n---\n\n**`frontend/src/contexts/AuthContext.tsx`**\n\n```typescript\nimport React, { createContext, useCallback, useContext, useMemo, useState } from 'react';\nimport type { User } from '@feastfite/shared';\nimport { authApi } from '../api/authApi';\n\ninterface AuthState {\n  user: User | null;\n  token: string | null;   // Stored in memory only — never localStorage\n  isAuthenticated: boolean;\n  isLoading: boolean;\n}\n\ninterface AuthContextValue extends AuthState {\n  login: (email: string, password: string) => Promise<void>;\n  logout: () => void;\n  register: (email: string, username: string, password: string) => Promise<void>;\n}\n\nconst AuthContext = createContext<AuthContextValue | null>(null);\n\nexport function AuthProvider({ children }: { children: React.ReactNode }) {\n  const [state, setState] = useState<AuthState>({\n    user: null,\n    token: null,\n    isAuthenticated: false,\n    isLoading: false,\n  });\n\n  const login = useCallback(async (email: string, password: string) => {\n    setState((s) => ({ ...s, isLoading: true }));\n    try {\n      const { user, token } = await authApi.login(email, password);\n      setState({ user, token, isAuthenticated: true, isLoading: false });\n    } catch (err) {\n      setState((s) => ({ ...s, isLoading: false }));\n      throw err;\n    }\n  }, []);\n\n  const logout = useCallback(() => {\n    setState({ user: null, token: null, isAuthenticated: false, isLoading: false });\n  }, []);\n\n  const register = useCallback(async (email: string, username: string, password: string) => {\n    setState((s) => ({ ...s, isLoading: true }));\n    try {\n      const { user, token } = await authApi.register(email, username, password);\n      setState({ user, token, isAuthenticated: true, isLoading: false });\n    } catch (err) {\n      setState((s) => ({ ...s, isLoading: false }));\n      throw err;\n    }\n  }, []);\n\n  const value = useMemo(\n    () => ({ ...state, login, logout, register }),\n    [state, login, logout, register]\n  );\n\n  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;\n}\n\nexport function useAuth(): AuthContextValue {\n  const ctx = useContext(AuthContext);\n  if (!ctx) throw new Error('useAuth must be used within <AuthProvider>');\n  return ctx;\n}\n```\n\nSecurity design note: JWT is stored in React state (in-memory), never in `localStorage` or `sessionStorage`. This prevents XSS token theft. The tradeoff is that refresh requires a re-login on page reload — address this in a later phase with a silent-refresh pattern using an HttpOnly cookie for the refresh token.\n\n---\n\n**`frontend/src/components/PrivateRoute.tsx`**\n\n```typescript\nimport { Navigate, Outlet, useLocation } from 'react-router-dom';\nimport { useAuth } from '../contexts/AuthContext';\n\nexport function PrivateRoute() {\n  const { isAuthenticated, isLoading } = useAuth();\n  const location = useLocation();\n\n  if (isLoading) {\n    return <div>Loading...</div>;  // Replace with a Spinner component later\n  }\n\n  if (!isAuthenticated) {\n    return <Navigate to=\"/login\" state={{ from: location }} replace />;\n  }\n\n  return <Outlet />;\n}\n```\n\n---\n\n**`frontend/src/api/authApi.ts`** (representative — others follow the same pattern)\n\n```typescript\nimport axios from 'axios';\nimport type { User } from '@feastfite/shared';\n\nconst client = axios.create({\n  baseURL: '/api/auth',\n  headers: { 'Content-Type': 'application/json' },\n});\n\ninterface AuthResponse {\n  user: User;\n  token: string;\n}\n\nexport const authApi = {\n  async login(email: string, password: string): Promise<AuthResponse> {\n    const { data } = await client.post<AuthResponse>('/login', { email, password });\n    return data;\n  },\n\n  async register(email: string, username: string, password: string): Promise<AuthResponse> {\n    const { data } = await client.post<AuthResponse>('/register', {\n      email,\n      username,\n      password,\n    });\n    return data;\n  },\n\n  async me(token: string): Promise<User> {\n    const { data } = await client.get<User>('/me', {\n      headers: { Authorization: `Bearer ${token}` },\n    });\n    return data;\n  },\n};\n```\n\nThe other API clients follow the same pattern with their own `baseURL`:\n- `territoryApi.ts`: `baseURL: '/api/territory'`\n- `voteApi.ts`: `baseURL: '/api/vote'`\n- `economyApi.ts`: `baseURL: '/api/economy'`\n- `profileApi.ts`: `baseURL: '/api/profile'`\n\n---\n\n**`frontend/src/App.tsx`**\n\n```typescript\nimport { BrowserRouter, Route, Routes } from 'react-router-dom';\nimport { AuthProvider } from './contexts/AuthContext';\nimport { PrivateRoute } from './components/PrivateRoute';\nimport { LoginPage } from './pages/LoginPage';\nimport { RegisterPage } from './pages/RegisterPage';\n\nfunction Dashboard() {\n  return <h1>Dashboard — TODO</h1>;\n}\n\nexport function App() {\n  return (\n    <BrowserRouter>\n      <AuthProvider>\n        <Routes>\n          <Route path=\"/login\" element={<LoginPage />} />\n          <Route path=\"/register\" element={<RegisterPage />} />\n          <Route element={<PrivateRoute />}>\n            <Route path=\"/\" element={<Dashboard />} />\n          </Route>\n        </Routes>\n      </AuthProvider>\n    </BrowserRouter>\n  );\n}\n```\n\n---\n\n**`frontend/src/pages/LoginPage.tsx`** (stub)\n\n```typescript\nimport { useState } from 'react';\nimport { useNavigate, useLocation, Link } from 'react-router-dom';\nimport { useAuth } from '../contexts/AuthContext';\n\nexport function LoginPage() {\n  const [email, setEmail] = useState('');\n  const [password, setPassword] = useState('');\n  const [error, setError] = useState<string | null>(null);\n  const { login, isLoading } = useAuth();\n  const navigate = useNavigate();\n  const location = useLocation();\n  const from = (location.state as { from?: Location })?.from?.pathname ?? '/';\n\n  async function handleSubmit(e: React.FormEvent) {\n    e.preventDefault();\n    setError(null);\n    try {\n      await login(email, password);\n      navigate(from, { replace: true });\n    } catch {\n      setError('Invalid email or password');\n    }\n  }\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <h1>Login to FeastFite</h1>\n      {error && <p style={{ color: 'red' }}>{error}</p>}\n      <input type=\"email\" value={email} onChange={(e) => setEmail(e.target.value)} placeholder=\"Email\" required />\n      <input type=\"password\" value={password} onChange={(e) => setPassword(e.target.value)} placeholder=\"Password\" required />\n      <button type=\"submit\" disabled={isLoading}>\n        {isLoading ? 'Logging in...' : 'Login'}\n      </button>\n      <Link to=\"/register\">Create an account</Link>\n    </form>\n  );\n}\n```\n\n`RegisterPage.tsx` follows the same pattern, calling `register(email, username, password)` instead.\n\n---\n\n### Phase G — Infrastructure\n\n---\n\n**`docker-compose.yml`**\n\n```yaml\nversion: \"3.9\"\n\nx-postgres-common: &postgres-common\n  image: postgres:16-alpine\n  restart: unless-stopped\n  environment:\n    POSTGRES_USER: ${POSTGRES_USER:-feastfite}\n    POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n  healthcheck:\n    test: [\"CMD-SHELL\", \"pg_isready -U ${POSTGRES_USER:-feastfite}\"]\n    interval: 10s\n    timeout: 5s\n    retries: 5\n\nservices:\n  # ── Databases ────────────────────────────────────────────────────\n  postgres-auth:\n    <<: *postgres-common\n    container_name: feastfite-postgres-auth\n    environment:\n      POSTGRES_USER: ${POSTGRES_USER:-feastfite}\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      POSTGRES_DB: auth_db\n    ports:\n      - \"5432:5432\"\n    volumes:\n      - postgres-auth-data:/var/lib/postgresql/data\n\n  postgres-territory:\n    container_name: feastfite-postgres-territory\n    image: postgis/postgis:16-3.4\n    restart: unless-stopped\n    environment:\n      POSTGRES_USER: ${POSTGRES_USER:-feastfite}\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      POSTGRES_DB: territory_db\n    ports:\n      - \"5433:5432\"\n    volumes:\n      - postgres-territory-data:/var/lib/postgresql/data\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U ${POSTGRES_USER:-feastfite}\"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n\n  postgres-vote:\n    <<: *postgres-common\n    container_name: feastfite-postgres-vote\n    environment:\n      POSTGRES_USER: ${POSTGRES_USER:-feastfite}\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      POSTGRES_DB: vote_db\n    ports:\n      - \"5434:5432\"\n    volumes:\n      - postgres-vote-data:/var/lib/postgresql/data\n\n  postgres-economy:\n    <<: *postgres-common\n    container_name: feastfite-postgres-economy\n    environment:\n      POSTGRES_USER: ${POSTGRES_USER:-feastfite}\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      POSTGRES_DB: economy_db\n    ports:\n      - \"5435:5432\"\n    volumes:\n      - postgres-economy-data:/var/lib/postgresql/data\n\n  postgres-profile:\n    <<: *postgres-common\n    container_name: feastfite-postgres-profile\n    environment:\n      POSTGRES_USER: ${POSTGRES_USER:-feastfite}\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      POSTGRES_DB: profile_db\n    ports:\n      - \"5436:5432\"\n    volumes:\n      - postgres-profile-data:/var/lib/postgresql/data\n\n  # ── Redis ─────────────────────────────────────────────────────────\n  redis:\n    image: redis:7-alpine\n    container_name: feastfite-redis\n    restart: unless-stopped\n    ports:\n      - \"6379:6379\"\n    volumes:\n      - redis-data:/data\n    command: redis-server --appendonly yes\n    healthcheck:\n      test: [\"CMD\", \"redis-cli\", \"ping\"]\n      interval: 10s\n      timeout: 5s\n      retries: 5\n\n  # ── RabbitMQ ──────────────────────────────────────────────────────\n  rabbitmq:\n    image: rabbitmq:3.13-management\n    container_name: feastfite-rabbitmq\n    restart: unless-stopped\n    ports:\n      - \"5672:5672\"\n      - \"15672:15672\"\n    environment:\n      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}\n      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS:-guest}\n    volumes:\n      - rabbitmq-data:/var/lib/rabbitmq\n    healthcheck:\n      test: [\"CMD\", \"rabbitmq-diagnostics\", \"-q\", \"ping\"]\n      interval: 15s\n      timeout: 10s\n      retries: 5\n\n  # ── MinIO ─────────────────────────────────────────────────────────\n  minio:\n    image: minio/minio:latest\n    container_name: feastfite-minio\n    restart: unless-stopped\n    command: server /data --console-address \":9001\"\n    ports:\n      - \"9000:9000\"\n      - \"9001:9001\"\n    environment:\n      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}\n      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin123}\n    volumes:\n      - minio-data:/data\n    healthcheck:\n      test: [\"CMD\", \"mc\", \"ready\", \"local\"]\n      interval: 15s\n      timeout: 10s\n      retries: 5\n\n  # ── Application Services ──────────────────────────────────────────\n  auth-service:\n    build:\n      context: .\n      dockerfile: services/auth-service/Dockerfile\n    container_name: feastfite-auth-service\n    restart: unless-stopped\n    ports:\n      - \"3001:3001\"\n    environment:\n      PORT: \"3001\"\n      NODE_ENV: development\n      DB_HOST: postgres-auth\n      DB_PORT: \"5432\"\n      DB_NAME: auth_db\n      DB_USER: ${POSTGRES_USER:-feastfite}\n      DB_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      REDIS_URL: redis://redis:6379\n      RABBITMQ_URL: amqp://${RABBITMQ_USER:-guest}:${RABBITMQ_PASS:-guest}@rabbitmq:5672\n      RABBITMQ_EXCHANGE: feastfite.events\n      JWT_SECRET: ${JWT_SECRET:-dev_secret_CHANGE_IN_PROD}\n      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-15m}\n    depends_on:\n      postgres-auth:\n        condition: service_healthy\n      redis:\n        condition: service_healthy\n      rabbitmq:\n        condition: service_healthy\n\n  territory-service:\n    build:\n      context: .\n      dockerfile: services/territory-service/Dockerfile\n    container_name: feastfite-territory-service\n    restart: unless-stopped\n    ports:\n      - \"3002:3002\"\n    environment:\n      PORT: \"3002\"\n      NODE_ENV: development\n      DB_HOST: postgres-territory\n      DB_PORT: \"5432\"\n      DB_NAME: territory_db\n      DB_USER: ${POSTGRES_USER:-feastfite}\n      DB_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      REDIS_URL: redis://redis:6379\n      RABBITMQ_URL: amqp://${RABBITMQ_USER:-guest}:${RABBITMQ_PASS:-guest}@rabbitmq:5672\n      RABBITMQ_EXCHANGE: feastfite.events\n      JWT_SECRET: ${JWT_SECRET:-dev_secret_CHANGE_IN_PROD}\n    depends_on:\n      postgres-territory:\n        condition: service_healthy\n      redis:\n        condition: service_healthy\n      rabbitmq:\n        condition: service_healthy\n\n  vote-service:\n    build:\n      context: .\n      dockerfile: services/vote-service/Dockerfile\n    container_name: feastfite-vote-service\n    restart: unless-stopped\n    ports:\n      - \"3003:3003\"\n    environment:\n      PORT: \"3003\"\n      NODE_ENV: development\n      DB_HOST: postgres-vote\n      DB_PORT: \"5432\"\n      DB_NAME: vote_db\n      DB_USER: ${POSTGRES_USER:-feastfite}\n      DB_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      REDIS_URL: redis://redis:6379\n      RABBITMQ_URL: amqp://${RABBITMQ_USER:-guest}:${RABBITMQ_PASS:-guest}@rabbitmq:5672\n      RABBITMQ_EXCHANGE: feastfite.events\n      JWT_SECRET: ${JWT_SECRET:-dev_secret_CHANGE_IN_PROD}\n      MINIO_ENDPOINT: minio\n      MINIO_PORT: \"9000\"\n      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}\n      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin123}\n    depends_on:\n      postgres-vote:\n        condition: service_healthy\n      redis:\n        condition: service_healthy\n      rabbitmq:\n        condition: service_healthy\n\n  economy-service:\n    build:\n      context: .\n      dockerfile: services/economy-service/Dockerfile\n    container_name: feastfite-economy-service\n    restart: unless-stopped\n    ports:\n      - \"3004:3004\"\n    environment:\n      PORT: \"3004\"\n      NODE_ENV: development\n      DB_HOST: postgres-economy\n      DB_PORT: \"5432\"\n      DB_NAME: economy_db\n      DB_USER: ${POSTGRES_USER:-feastfite}\n      DB_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      REDIS_URL: redis://redis:6379\n      RABBITMQ_URL: amqp://${RABBITMQ_USER:-guest}:${RABBITMQ_PASS:-guest}@rabbitmq:5672\n      RABBITMQ_EXCHANGE: feastfite.events\n      JWT_SECRET: ${JWT_SECRET:-dev_secret_CHANGE_IN_PROD}\n    depends_on:\n      postgres-economy:\n        condition: service_healthy\n      redis:\n        condition: service_healthy\n      rabbitmq:\n        condition: service_healthy\n\n  profile-service:\n    build:\n      context: .\n      dockerfile: services/profile-service/Dockerfile\n    container_name: feastfite-profile-service\n    restart: unless-stopped\n    ports:\n      - \"3005:3005\"\n    environment:\n      PORT: \"3005\"\n      NODE_ENV: development\n      DB_HOST: postgres-profile\n      DB_PORT: \"5432\"\n      DB_NAME: profile_db\n      DB_USER: ${POSTGRES_USER:-feastfite}\n      DB_PASSWORD: ${POSTGRES_PASSWORD:-devpassword}\n      REDIS_URL: redis://redis:6379\n      RABBITMQ_URL: amqp://${RABBITMQ_USER:-guest}:${RABBITMQ_PASS:-guest}@rabbitmq:5672\n      RABBITMQ_EXCHANGE: feastfite.events\n      JWT_SECRET: ${JWT_SECRET:-dev_secret_CHANGE_IN_PROD}\n    depends_on:\n      postgres-profile:\n        condition: service_healthy\n      redis:\n        condition: service_healthy\n      rabbitmq:\n        condition: service_healthy\n\n  # ── Kong API Gateway ──────────────────────────────────────────────\n  kong:\n    image: kong:3.6\n    container_name: feastfite-kong\n    restart: unless-stopped\n    environment:\n      KONG_DATABASE: \"off\"\n      KONG_DECLARATIVE_CONFIG: /etc/kong/kong.yml\n      KONG_PROXY_ACCESS_LOG: /dev/stdout\n      KONG_ADMIN_ACCESS_LOG: /dev/stdout\n      KONG_PROXY_ERROR_LOG: /dev/stderr\n      KONG_ADMIN_ERROR_LOG: /dev/stderr\n      KONG_ADMIN_LISTEN: \"0.0.0.0:8001\"\n      KONG_PROXY_LISTEN: \"0.0.0.0:8000\"\n    ports:\n      - \"8000:8000\"   # Proxy (external client traffic)\n      - \"8001:8001\"   # Admin API (do not expose in production)\n    volumes:\n      - ./services/api-gateway/kong.yml:/etc/kong/kong.yml:ro\n    depends_on:\n      - auth-service\n      - territory-service\n      - vote-service\n      - economy-service\n      - profile-service\n\nvolumes:\n  postgres-auth-data:\n  postgres-territory-data:\n  postgres-vote-data:\n  postgres-economy-data:\n  postgres-profile-data:\n  redis-data:\n  rabbitmq-data:\n  minio-data:\n```\n\nKey docker-compose decisions:\n1. Named volumes (not bind mounts) for all databases — bind mounts cause permission issues on Windows with WSL2/Docker Desktop.\n2. `condition: service_healthy` in `depends_on` ensures services only start after their dependencies pass healthchecks. This prevents the common `ECONNREFUSED` race condition on first `docker-compose up`.\n3. The YAML anchor `x-postgres-common` deduplicates the four standard Postgres configs. The territory Postgres overrides the `image:` so it cannot use the anchor directly.\n4. Kong config is mounted as a read-only volume (`kong.yml:ro`) rather than baked into the image — this allows editing `kong.yml` without rebuilding the Kong image.\n\n---\n\n**`k8s/namespace.yml`**\n\n```yaml\napiVersion: v1\nkind: Namespace\nmetadata:\n  name: feastfite\n  labels:\n    app.kubernetes.io/managed-by: manual\n    environment: development\n```\n\n---\n\n**`k8s/infra/postgres.yml`** (representative — one StatefulSet per DB)\n\n```yaml\napiVersion: apps/v1\nkind: StatefulSet\nmetadata:\n  name: postgres-auth\n  namespace: feastfite\nspec:\n  serviceName: postgres-auth\n  replicas: 1\n  selector:\n    matchLabels:\n      app: postgres-auth\n  template:\n    metadata:\n      labels:\n        app: postgres-auth\n    spec:\n      containers:\n        - name: postgres\n          image: postgres:16-alpine\n          env:\n            - name: POSTGRES_DB\n              value: auth_db\n            - name: POSTGRES_USER\n              valueFrom:\n                secretKeyRef:\n                  name: postgres-credentials\n                  key: username\n            - name: POSTGRES_PASSWORD\n              valueFrom:\n                secretKeyRef:\n                  name: postgres-credentials\n                  key: password\n          ports:\n            - containerPort: 5432\n          volumeMounts:\n            - name: data\n              mountPath: /var/lib/postgresql/data\n  volumeClaimTemplates:\n    - metadata:\n        name: data\n      spec:\n        accessModes: [\"ReadWriteOnce\"]\n        resources:\n          requests:\n            storage: 5Gi\n---\napiVersion: v1\nkind: Service\nmetadata:\n  name: postgres-auth\n  namespace: feastfite\nspec:\n  selector:\n    app: postgres-auth\n  ports:\n    - port: 5432\n      targetPort: 5432\n  clusterIP: None  # Headless service for StatefulSet DNS\n```\n\nThe `k8s/infra/` files for redis, rabbitmq, and minio follow the same StatefulSet + headless Service pattern. These are intentionally minimal — in real Kubernetes you would use Helm charts (bitnami/postgresql, bitnami/redis, etc.).\n\n---\n\n## Part 4 — Developer Parallelism Strategy\n\nOnce Phase A and Phase B are complete (root + shared package), 4 developers can work in parallel:\n\n| Dev | Work Package |\n|-----|-------------|\n| Dev 1 | `services/auth-service` — full implementation (JWT issuance, pg schema, Redis sessions) |\n| Dev 2 | `services/territory-service` — PostGIS queries, territory claim logic, event publishing |\n| Dev 3 | `services/vote-service` + `services/economy-service` — vote session state machine, points ledger |\n| Dev 4 | `frontend` — React components, routing, API client integration |\n\nProfile-service can be picked up by any dev who finishes early, as it has no cross-service dependencies at the stub phase.\n\n**Conflict zones to communicate about:**\n- `packages/shared/src/types/index.ts` — all devs will want to add fields. Establish that type additions are append-only (no breaking changes to existing interfaces) and require a short Slack thread before merging.\n- `docker-compose.yml` — only infra changes; agree that service additions are the only permitted changes during parallel dev phase.\n- `services/api-gateway/kong.yml` — route additions only; no changes to existing plugin config.\n\n---\n\n## Part 5 — Verification Checklist\n\n### Step 1: Local typecheck (no Docker needed)\n\n```bash\n# From repo root, after creating all files:\nnpm install\nnpm run build -w packages/shared\nnpm run typecheck   # should report 0 errors\n```\n\n### Step 2: Individual service health checks\n\n```bash\n# Terminal 1 (start infra only first):\ndocker-compose up postgres-auth redis rabbitmq -d\n\n# Terminal 2:\nnpm run dev -w services/auth-service\n# Expected: \"[auth-service] listening on port 3001\"\n\ncurl http://localhost:3001/health\n# Expected: {\"status\":\"ok\",\"service\":\"auth-service\"}\n```\n\n### Step 3: Full Docker Compose stack\n\n```bash\ndocker-compose up --build\n```\n\nWait for all healthchecks to pass (roughly 60 seconds on first run due to PostGIS initialization). Then:\n\n```bash\n# Health check all services via Kong\ncurl http://localhost:8000/api/auth/health   # → {\"status\":\"ok\",\"service\":\"auth-service\"}\ncurl http://localhost:8000/api/territory/health\ncurl http://localhost:8000/api/vote/health\ncurl http://localhost:8000/api/economy/health\ncurl http://localhost:8000/api/profile/health\n\n# Kong admin (list configured routes)\ncurl http://localhost:8001/routes | jq '.data[].name'\n\n# RabbitMQ management UI\nopen http://localhost:15672  # guest/guest\n\n# MinIO console\nopen http://localhost:9001  # minioadmin/minioadmin123\n```\n\n### Step 4: Frontend dev server\n\n```bash\nnpm run dev -w frontend\n# Opens http://localhost:5173\n# /login and /register routes should render\n# / should redirect to /login\n```\n\n### Step 5: Validate PostGIS\n\n```bash\ndocker exec -it feastfite-postgres-territory psql -U feastfite -d territory_db \\\n  -c \"SELECT PostGIS_Version();\"\n# Expected: \"3.4 ...\" — confirms PostGIS is active\n```\n\n---\n\n## Part 6 — Common Gotchas Reference\n\n| Gotcha | Problem | Fix |\n|--------|---------|-----|\n| `@feastfite/shared` not found at runtime | `dist/` not built before Docker build | Always run `npm run build -w packages/shared` in CI before building service images |\n| `ECONNREFUSED` on RabbitMQ at service startup | Race condition: service starts before RabbitMQ is ready | The `amqp.ts` reconnect loop handles this — just set `depends_on: rabbitmq: condition: service_healthy` |\n| Kong returns 401 on `/health` | Global JWT plugin enabled | Disable JWT plugin globally; apply it only to authenticated routes via `route:` scoping in `kong.yml` |\n| PostGIS `CREATE EXTENSION` fails | Extension not pre-loaded | `postgis/postgis:16-3.4` auto-creates it, but only for the default DB. Run `CREATE EXTENSION IF NOT EXISTS postgis;` in migration scripts for `territory_db` |\n| Windows bind mount permission errors | Docker Desktop + Windows NTFS | Use named volumes (already done in `docker-compose.yml`) instead of `./data:/var/lib/postgresql/data` bind mounts |\n| npm workspace install installs wrong version | Running `npm install` in a service subdirectory | Always run `npm install` from the repo root. Per-subdirectory installs bypass hoisting |\n| Vite HMR breaks behind Kong proxy | WebSocket HMR traffic not proxied | Vite dev server runs on 5173 directly; only API calls proxy through Kong. Do not proxy `localhost:5173` through Kong |\n| `tsx watch` fails on Windows | Path separators in globs | Use `tsx watch src/index.ts` (explicit path, not glob) — already specified in all service `dev` scripts |\n| Kong `_format_version: \"2.1\"` warning | Deprecated format in Kong 3.x | Use `\"3.0\"` — already specified in the plan |\n| `docker-compose up` hangs on PostGIS | `postgis/postgis:16-3.4` initializes extensions on first start, slow | The healthcheck uses `pg_isready` which returns true before extension init completes. Add a 5-second `start_period` to the PostGIS healthcheck |\n\n---\n\n## Part 7 — Files NOT Created (Intentional Omissions)\n\nThese are deferred to after the parallel dev phase:\n\n- **Migration scripts** (`services/<name>/migrations/`) — schema not finalized\n- **Unit/integration tests** — no test runner configured yet (Vitest recommended)\n- **CI pipeline** (`.github/workflows/`) — deferred until services compile cleanly\n- **Kubernetes service/deployment YAMLs** — only namespace + infra placeholders now\n- **MinIO bucket initialization script** — use a one-time job or the console at first\n- **ESLint/Prettier config** — add after parallel dev phase to avoid merge conflicts on formatting\n\n---\n\n### Critical Files for Implementation\n\n- `/c/Users/creat/Desktop/learning/scope/FeastFite/package.json`\n- `/c/Users/creat/Desktop/learning/scope/FeastFite/tsconfig.base.json`\n- `/c/Users/creat/Desktop/learning/scope/FeastFite/packages/shared/src/utils/amqp.ts`\n- `/c/Users/creat/Desktop/learning/scope/FeastFite/docker-compose.yml`\n- `/c/Users/creat/Desktop/learning/scope/FeastFite/services/api-gateway/kong.yml`"
  }
]